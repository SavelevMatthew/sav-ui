import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs'
import { colors } from '@savelevmatthew/ui/colors'

<Meta title={'colors'} />

<ColorPalette>
    <ColorItem
        title={'colors.DOM.brand'}
        subtitle={''}
        colors={Object.assign({},
            ...Object.keys(colors.DOM.brand).map((key) => ({[key]: colors.DOM.brand[key].value}))
        )}
    />
</ColorPalette>