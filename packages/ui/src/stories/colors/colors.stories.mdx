import { Meta, ColorPalette, ColorItem, Story } from '@storybook/addon-docs'

<Meta title="colors" />

<Story name="palette">
    {() => (
        <ColorPalette>
            <ColorItem title={'Test color'} subtitle={'Used for test purposes'} colors={{ red: '#f75' }} />
        </ColorPalette>
    )}
</Story>